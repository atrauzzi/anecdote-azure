#!/bin/bash

for dir in ./functions/*
do

    DIR=${dir%*/}

    cd $DIR

    yarn install \
        --no-progress \
        --no-bin-links \
        --ignore-engines \
        --production

    yarn clean

    cd ..
    cd ..
done
